# Flache node_modules (wie npm/Yarn Classic). Verhindert extrem lange .pnpm-Pfade; NSIS/makensis unter
# Windows kann sonst keine !include zu app-builder-lib auflösen (electron-userland/electron-builder#8346).
node-linker=hoisted
