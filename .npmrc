# electron-builder + NSIS (Windows): makensis kann keine !include-Pfade öffnen, wenn .pnpm-Pfade zu lang sind
# (electron-userland/electron-builder#8346). Kurzer Store + Hoist von app-builder-lib an die node_modules-Wurzel.
virtual-store-dir-max-length=50
public-hoist-pattern[]=*app-builder-lib*
