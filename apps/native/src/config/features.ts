/**
 * `true`: load the Next.js app (`apps/web`) inside a WebView — same DOM and `@repo/ui` as the browser.
 * `false`: render the NativeWind + local `components/ui` screen (offline-friendly).
 */
export const USE_WEBVIEW = true;
